




jicofo {
    
    authentication {
      enabled = true
      // The type of authentication. Supported values are XMPP or JWT.
      
      type = XMPP
      
      login-url = "jitsi.pydev.my.id"
      enable-auto-login = true
      authentication-lifetime = 24 hours
    }
    

    // Configuration related to jitsi-videobridge
    bridge {
      

      

      

      
      
      

      

      
      

      
      brewery-jid = "jvbbrewery@internal-muc.jitsi.pydev.my.id"
      

      
    }
    // Configure the codecs and RTP extensions to be used in the offer sent to clients.
    codec {
      video {
        
        
        
        
      }
      audio {
        
      }
      rtp-extensions {
        video-layers-allocation {
          enabled = false
        }
        mid {
          enabled = true
        }
      }
    }

    conference {
      

      

      

      max-ssrcs-per-user = "20"

      max-ssrc-groups-per-user = "20"

      

      

      

      

      

      

      

      

      
    }

    

    

    

    

    

    octo {
      // Whether or not to use Octo. Note that when enabled, its use will be determined by
      // $jicofo.bridge.selection-strategy. There's a corresponding flag in the JVB and these
      // two MUST be in sync (otherwise bridges will crash because they won't know how to
      // deal with octo channels).
      enabled = false
      sctp-datachannels = true
    }

    

    sctp {
      enabled = true
    }

    xmpp {
      
      client {
        enabled = true
        hostname = "prosody"
        port = "5222"
        domain = "auth.jitsi.pydev.my.id"
        xmpp-domain = "jitsi.pydev.my.id"
        username = "focus"
        password = "jitsi123"
        conference-muc-jid = "muc.jitsi.pydev.my.id"
        client-proxy = "focus.jitsi.pydev.my.id"
        disable-certificate-verification = true
      }
      

      trusted-domains = [  ]

    }
}

include "custom-jicofo.conf"
